#include <stdio.h>
#include <stdlib.h>


// central idea: Use void pointers as parameters on filters and a type enum
// to just allocate memory and realize their type inside with switch statement. 

typedef enum {
    INT,
    FLOAT,
    CHAR,
    STRING
} Type;

int main () {



    return 0;
}